apiVersion: v1
kind: Service
metadata:
  labels:
    app: url-shortener
  name: url-shortener
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: url-shortener
